# Loops 9-12 Complete

## Loop 9 - Real wallet adapter
- Phantom, Solflare, Backpack detection
- Multi-wallet picker modal
- Install-wallet fallback when no extension
- Auto-reconnect if already trusted
- `DEMO_MODE = false` in `js/wallet.js`

## Loop 10 - NFT image system
- `js/nft-images.js` auto-loads `assets/images/nfts/{id}.webp`
- Falls back to seeded picsum placeholders
- Collection modal + grid use live images
- See `assets/images/nfts/README.md`

## Loop 11 - Competitor-informed copy
- Researched Mad Lads / xNFT / holder-identity patterns
- Updated `LORE` in `collection-data.js`
- Sharpened hero + mint copy (identity over JPEG)

## Loop 12 - Deploy + OG
- `vercel.json` + `netlify.toml`
- `assets/og-image.svg` social preview
- OG/Twitter meta on index
- `js/site-config.js` for site URL

## Deploy

**Vercel:** drag folder or `vercel --prod`

**Netlify:** drag folder or connect repo, publish directory = `.`

Update `SITE.url` in `js/site-config.js` when domain is live.
